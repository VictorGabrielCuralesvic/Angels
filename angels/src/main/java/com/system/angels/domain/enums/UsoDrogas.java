package com.system.angels.domain.enums;

public enum UsoDrogas {
    ABSTINENCIA_TOTAL(0),
    USO_EXPERIMENTAL(1),
    USO_RECREATIVO(2),
    USO_PROBLEMATICO(3),
    DEPENDENCIA(4);

    UsoDrogas(final int codigo) {
    }
}

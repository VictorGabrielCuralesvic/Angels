CREATE TABLE exame (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   acompanhamento_id BIGINT NOT NULL,
   tipo VARCHAR(255) NOT NULL,
   resultado VARCHAR(255) NOT NULL,
   observacao VARCHAR(255),
   CONSTRAINT pk_exame PRIMARY KEY (id)
);

ALTER TABLE exame ADD CONSTRAINT FK_EXAME_ON_ACOMPANHAMENTO FOREIGN KEY (acompanhamento_id) REFERENCES acompanhamento (id);